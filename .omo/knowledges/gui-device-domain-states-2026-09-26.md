# Direct device entrance without a unique domain

Device entrances now distinguish unconfirmed network identity, no eligible domain with two networks, and ambiguous
multiple domain candidates. These use disabled authority and localized explanations rather than indefinite pending.
Graph empty-state text and the scope caption explain the same condition. Domain browsers are disabled while the local
Endpoint inspector continues displaying the actual endpoint identity, native network and claim details.

`DeviceDomainAvailabilityTest` covers the classifier branches. Actual-client `ui.endpoint` places a real isolated,
powered production Endpoint at the original endpoint's east(4).south(3), with its own creative energy cell. Waiting
for a confirmed native network and an actual `forDevice` no-domain result validates the state without fabricated rows.
The client verifies actual identity/network text, disabled domain navigation and policy editing, and the graph explanation.
Cleanup removes only the fixture's recorded positions. Do not place the cell adjacent to the second observed endpoint:
that would join the networks and invalidate the premise.

Initial evidence: `.omo/evidence/gui-device-domain-states/attempt-20260926T132239002Z`; 6/6 scenarios, 1189 steps,
109 checks; 246 unit tests pass. Subsequently exercised unchanged under the light theme in
`.omo/evidence/gui-ae-visual-style-refined/attempt-20260926T133042577Z` with the same passing totals.

No real-client ambiguous-domain or unconfirmed-identity fixture was exercised in this iteration. Those branches have
classifier coverage and localized presentation only; do not report them as visually verified.
