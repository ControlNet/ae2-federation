# Task 37 scale qualification blocker

## 2026-09-25 opt-in Federation warmup stage measurement

- A fresh 720-file source snapshot with manifest SHA-256 `143cb77b0f584ad5a873341fd6af55499e82a0b5baa5a10666efb8cf379478a4` produced one real failed 300-second Federation warmup: 27 distinct native UUIDs/Drive receipts and 432 accepted units in 301.750062433 seconds / 5,187 server ticks. Stage intervals: target input/context 124.964219562 seconds and machine/callback 176.521402569 seconds, versus planning/submission/readback 0.264440302 seconds combined. These are transition-to-transition wall intervals that include test fixture and native waiting, not isolated product CPU costs. The 256-job guard failed, nested Java/Gradle and outer Gradle exited nonzero, no sample/result exists, and finalizer removed the owned world/lock. Exact evidence and the preflight staging mistakes: `.omo/evidence/task-37-federation-stage-run/verification.md`.
- The inherited transfer manifest included live `run-manual-client` and `run-server` trees. Its first abandoned, unrun task-owned snapshot contains copies of these files; do not reuse it or infer source parity. The successful diagnostic used a separate filtered source snapshot and did not touch local user worlds. The copied runtime files remain on the approved VM pending an explicit retention decision; no existing snapshot was deleted.

## 2026-09-24 remote current-source three-layout correctness

- Exact sixteen-working-target direct/native-subnet/Federation replays completed serially on a fresh immutable 739-file snapshot. Each selected Gradle process exited zero, passed its one required GameTest, audited 256 physical jobs/UUIDs/Drive receipts and 4096 units, and left no world or task-owned Java process. Snapshot pre/post source digest matched. Live local source changed concurrently in verifier/docs files after capture, so final live-local-to-snapshot parity is not claimed. Full path, hashes and limits: `.omo/evidence/task-37-remote-current-three-layout/verification.md`. These are NON_COMPARABLE correctness runs, not timed resource-matched Task 37 profiles; Tasks 37/38/40/F1-F4 remain `[~]`.

## 2026-09-24 superseding Task 37/38 host interpretation

- The earlier strict `NOT_YET_ELIGIBLE` preflight remains historically accurate, but the user's override permits best-effort reserved-VM Task 37 indicative order-of-magnitude measurements and Task 38's two-hour soak. Unreadable KVM governors, absent exclusive cpuset and runnable count above 2 are limitations, not hard gates. Log `wakatime-cli`, `videnoa`, `herdr` and other observed competitors, plus JVM/CPU/affinity/load with each repeated sample; reservation cannot guarantee zero interference.
- Preserve native correctness, conservation, resource accounting, provenance, Task 37's 300/600-second windows and three repetitions, and the Task 38 soak duration. Intrinsically or materially mismatched resources stay non-comparable for strict speedup; caveated elapsed ratios may be descriptive. Workload/profile and independent resource-verifier gaps below remain blockers to final qualification. No measurements or controlled regression baseline result from this decision.

## 2026-09-24 sixteen-target physical Federation 256 continuation

- The manual-only independent Federation replay now covers the same 256 distinct physical Patterns and 4096 native machine/callback/Drive units as direct/subnet-256, over sixteen separate claimed Bridge/Fabric/Policy/Endpoint target Grids. A settled missing-last-Pattern red, complete passing long-lived GameTest transcript and serial source/Federation-16/direct/subnet-256 controls are bound by SHA-256 in `.omo/evidence/task-37-large-federation-256-development/verification.md`. The selected detached launcher did not capture its shell exit, although Gradle reported success and cleanup. This closes only serial correctness; Bridge/Endpoint/target-cell/creative-power additions and unmeasured energy, total chunks, peak demand and timed repetitions prohibit resource-parity or benchmark claims. Tasks 37/38/40/F1-F4 remain `[~]`.

## 2026-09-24 large native-subnet 256 development continuation

- The independent 16-target native-subnet 256 manual replay has a settled missing-last-Pattern red and a passing process with 256 distinct native link UUIDs, 4096 typed machine/callback units and 4096 actual source Drive units. The direct-256 and old subnet-16 controls independently pass. Process log hashes, source binding and energy/cell/chunk differences are in `.omo/evidence/task-37-large-subnet-256-development/verification.md`. This completes only serial correctness; sixteen additional target Grids/cells/creative energy cells prevent resource-parity or speedup claims, and no repeated timed late/ultra profile or perf-case verifier exists.

- The existing `federationBenchmark` accepts a single exact ID from `tests/scenarios/manifest.json`, not a comma-separated profile list. Only smoke, processing-small, mixed-small, and ui-small exist. `-Pprofile=small,late,ultra` fails before evidence attempt creation.
- The planned `perf.resource-integrity`, `perf.reject-incomparable-speedup`, and `perf.reject-budget-regression` IDs are absent from the manifest; the wrapper rejects them before launching GameTests.
- Task 20's processing baseline is correctness/resource-only. Task 30's measured iterations are short native mixed scenes, not 5-minute/10-minute three-layout windows. Neither should be promoted as Task 37 timing evidence.
- Three tiers times three layouts times three repetitions times 15 minutes is 405 minutes minimum serial window time, excluding startup. The native shared world lock requires serialization. A shared, unpinned host cannot freeze stable reference timing medians even if correctness runs succeed.
- Preserve schema-v3 current-source identity and lifecycle when adding scale support. Profiles, layout-specific native children, independent resource equations, actual tick/wall samples, and strict negative perf cases are prerequisites to a PASS claim.

## 2026-09-24 four-target Federation replay boundary

- The manual-only four-target Federation 256 replay now passes 64 serial 16-unit jobs on each of four physical targets, with 256 unique native links, exclusive D high-slot authority and 4096 actual units in five source Drive cells. Its process-scoped audit and older selector controls are recorded in `.omo/evidence/task-37-federation-four-target-256-development/verification.md`. Do not treat this correctness run as a 16-target topology, concurrent sample, matched small/late/ultra windows, perf-case result or Task 37 completion.

## 2026-09-23 incremental continuation

- The Gradle entrypoint now splits and prevalidates comma-separated registered profile IDs, rejecting empty/duplicate/unknown members before any child starts. Each accepted member still invokes the original single-profile source-bound producer independently and serially; no composite report semantics were claimed.
- Three JSON tier declarations bind the target grids, patterns, order units, 300/600-second windows, three repetitions, and layout names. They are not scene outputs. The scale contract test was red on absent files before the declarations made it green.
- A real Task 20 native development child passed one required GameTest and wrote `benchmark-native.properties` under `.omo/evidence/task-37-development/attempt-20260923T082743302Z`, but the outer result failed Task 20 capture identity: baseline dependency-lock hash `74bdb2dc...` versus current `gradle/verification-metadata.xml` hash `2677d763...`. Do not treat this incomplete attempt as accepted evidence or change Task 20's baseline without its owner and before/after proof.
- Final registered-profile dispatch smoke: `.omo/evidence/task-37-dispatch-final/attempt-20260923T083414303Z/result.json`; one real required GameTest passed and `federationVerifyEvidence` consumed its schema-v3 result against the then-current source. It does not qualify a Task 37 tier. The exact Task 37 command and three perf cases still reject missing registrations before native execution.
- After the tracked documentation correction, a fresh current-source registered-profile attempt `.omo/evidence/task-37-dispatch-final-current/attempt-20260923T083614900Z/result.json` and its persisted consumer both passed. This is a harness smoke only; no scale sample or perf-case result exists.

## Native topology continuation

- `ScaleFactoryProfile` loads the three tier declarations in the GameTest child. Earlier small and late topology checks counted real powered AE2 Grids, but the earlier ultra 512-Grid pass used prohibited forced chunk tickets and must be discarded as evidence. The stand-alone 16-in/4-out storage assertions were removed because they cannot qualify Processing.
- A y-step of two merged adjacent energy/chest pairs into 64 instead of 128 Grids; extending z outside the bounded footprint left nodes inactive. The safety repair confines x/z to 1..15 (maximum 320 isolated positions) and removes all Task 37 `setChunkForced` calls, `ownedForcedChunkCount`, and ticket-release assertions. `.omo/evidence/task-37-safety-small/` passes 16-Grid topology; `.omo/evidence/task-37-safety-ultra/` fails before placement with `Scale tier exceeds bounded physical Grid fixture capacity`. Ultra is unsupported in this fixture, not a benchmark failure or speedup.
- Three attempted small planner/catalog probes failed before planner assertions at `Waiting for small native Grids`: `.omo/evidence/task-37-small-planner-attempt/`, `.omo/evidence/task-37-small-planner-staged/`, `.omo/evidence/task-37-small-planner-forced/`. The experimental fixture was rolled back. Do not register topology-only tests as scale profiles. Native CPU, Provider, machine, returns, logical Pattern population, three comparable layouts, per-key accounts, and 300/600-second repeated windows remain missing.

## 2026-09-23 per-node diagnosis and native-development job

- The new exact manual-only `scalesmallprocessingdevelopment` GameTest was red before reserving source positions: all 16 AE2 chests were active with confirmed `NetworkId`s at `before-provider`; immediately after Task20's Provider placement `(1,2,3)` changed from an ME chest to an isolated creative energy cell, and `(3,2,3)` changed to a vanilla host chest. Both lost their scale-chest nodes/Grids/identities. It then failed with `Waiting for small native Grids after Provider placement`. This is an observed spatial overwrite, not an inferred identity/boot defect.
- The scale lattice now omits the six positions `y=2, z in {1,3}, x in {1,3,5}` for the Task20 source, isolated energy, target and CPU footprint. Bounded capacity is **314**, not the former 320; ultra 512 remains unsupported and no chunk tickets are used. With this exclusion the same test passed through Provider placement, confirmed source identity, seeded Task20 physical storage/CPU construction and native connection; all 16 auxiliary scale chests survived. The source is an additional native Grid, so this scene is **not** a 16-Grid comparable layout.
- The development scene then executed one real AE2 calculation and tracked CPU job through the Pattern Provider and Task30 registered item-handler machine. Its native `push-return` and `return-inject` receipts were emitted; the machine transition produced one diamond, the requester callback accepted one, and the physical ME cell held one diamond with zero remaining cobblestone, zero machine input, empty native return inventory and idle CPU. This is a one-job integration probe, not the profile's 1,000-10,000-unit replay or a three-layout result. It deliberately ends with one required failure: `Small native-subnet and Federation Processing layouts remain unqualified`. Do not promote it to Task37 qualification or claim native-subnet non-comparability; neither was constructed.
- The opt-in per-node receipt `.omo/evidence/task-37-small-development/scale-small-nodes.log` has 108 `AE2F_SCALE_NODE` records, including every auxiliary chest before/after Provider and CPU placement and the Provider/source chest/CPU nodes at their distinct stages. It includes position, block, node object, booted/active, native Grid object and confirmed/unconfirmed `NetworkId`. Before CPU connection the new chest and CPU were on a temporary inactive Grid with unconfirmed identity; after connection they and the Provider shared the same active native Grid and confirmed ID. Do not require immediate identity settlement from a just-placed node.
- Red command (before footprint reservation): `./gradlew :neoforge-1.21.1:runGameTestServer -PfederationGameTestSelection=benchmark -PfederationGameTestId=scalesmallprocessingdevelopment --no-configuration-cache --dependency-verification=strict` -> `1 required tests failed`, `Waiting for small native Grids after Provider placement`. With reserved coordinates, the then-topology-only stage passed `All 1 required tests passed :)`; it was subsequently extended and is **intentionally red** until three authentic layouts exist.
- Final focused command: `./gradlew :neoforge-1.21.1:runGameTestServer -PfederationGameTestSelection=benchmark -PfederationGameTestId=scalesmallprocessingdevelopment -PfederationNativeEvidenceFile=$PWD/.omo/evidence/task-37-small-development/benchmark-native.properties --no-configuration-cache --dependency-verification=strict` -> native one-job receipt as above, followed by exactly `1 required tests failed` for the explicit unqualified-layout assertion; outer Gradle exits nonzero. No fabricated native success banner.
- `./gradlew :neoforge-1.21.1:test --tests '*ScaleBenchmarkProfileContractTest' --rerun-tasks --no-configuration-cache --dependency-verification=strict` and `./gradlew :neoforge-1.21.1:test check build --no-configuration-cache --dependency-verification=strict` both exited 0. Java LSP diagnostics on changed source files were empty. `ps -eo pid,comm,args | rg '[j]ava.*(forgeserverdev|GameTestServer|runGameTestServer)'` returned no owned process; `federationCleanupGameTest` ran and `neoforge-1.21.1/run-gametest` no longer exists (no owned world/lock remains). The first failing red run generated a crash during an attempted Task30 aggregate snapshot without the Task30 automation observer; a narrow machine-transition accessor replaced that call, and the final run failed only for the deliberate unqualified-layout assertion.

## Native-subnet and Federation development continuation

- The serial small development GameTest now completes one authentic native-big-grid job and one authentic native-subnet job. Each run logged `plannerComplete=true jobs=1 input=1 finalInput=0 machineOutput=1 callback=1 physicalOutput=1`; native-subnet has a separate Interface, physical cell, Export Bus, and registered item-handler machine. The fixture checks 16 distinct active Grids (source plus 14 auxiliary plus subnet). Neither job is a timed benchmark sample.
- Three Federation target attempts did not establish a confirmed target identity: first without explicit target seeds, next with Endpoint/chest seeds, and finally with Endpoint/chest/Export Bus seeds. The last focused run failed at `target-identity-pending` with active Endpoint and chest on the same Grid, both confirmed identities empty, and `IdentitySettlement[status=AMBIGUOUS_SPLIT, networkId=Optional.empty]`. The cause of the identity ambiguity is not established. The experimental Federation target code was removed; the small development GameTest deliberately remains red with `Small Federation Processing layout remains unqualified` after completing both native jobs and closing its resources. No three-layout claim or performance qualification is warranted.
- Focused command: `./gradlew :neoforge-1.21.1:runGameTestServer -PfederationGameTestSelection=benchmark -PfederationGameTestId=scalesmallprocessingdevelopment --no-configuration-cache --dependency-verification=strict`. It produced the two native receipts above, then exactly one expected required failure for the unqualified Federation layout; `federationCleanupGameTest` ran. `./gradlew :neoforge-1.21.1:test check build --no-configuration-cache --dependency-verification=strict` exited 0; changed Java file diagnostics and `git diff --check` were clean. No owned GameTest Java process or `neoforge-1.21.1/run-gametest` remained.

## Staged Federation target identity development

- The selected manual-only `scalesmallfederationidentitydevelopment` runs the two authentic native one-job probes, then constructs a separate physical target ME Chest with cell and energy, lets that anchor settle, places and pre-tick-seeds the Endpoint from its confirmed ID, and separately places/seeds/connects an AE2 Export Bus. The first staged unseeded Endpoint experiment also settled on the anchor Grid with a distinct node UUID, so a separate-lineage explanation for that specific staged placement was falsified. The older simultaneous target attempts did not identify a competing Grid and remain unexplained; do not infer their cause from this run.
- Final selected run: `./gradlew :neoforge-1.21.1:runGameTestServer -PfederationGameTestSelection=benchmark -PfederationGameTestId=scalesmallfederationidentitydevelopment -PfederationNativeEvidenceFile=$PWD/.omo/evidence/task-37-staged-identity-final/benchmark-native.properties --no-configuration-cache --dependency-verification=strict`. It emitted `All 1 required tests passed :)`, `BUILD SUCCESSFUL`, and `federationCleanupGameTest`; the three `AE2F_SCALE_IDENTITY` receipts are at `.omo/evidence/task-37-staged-identity-final/scale-small-identity.log`.
- In that run the source remained on Grid `2077132114` with ID `ca42c675-e89b-48b9-b83d-f78bb704f25f`; anchor, Endpoint, and bus were active and `SETTLED` on Grid `1480891573` with ID `a12c7b33-ba5e-444c-aaf0-951ab67334d7`. Their node UUIDs were distinct, and the target ID's registry claims were on only that Grid at all three checkpoints. The fixture asserted 16 distinct active Grids including source and target. `registryGridCount=17` counts registered Grids, not this active-Grid assertion; do not treat it as a competing target claim. Source/target physical cell snapshots were zero after both native outputs were removed. Each native job independently logged `jobs=1 input=1 finalInput=0 machineOutput=1 callback=1 physicalOutput=1`.
- This is identity-only success. No physical Federation Bridge/Fabric, authorized Processing route, third job, full 256-pattern/4,096-unit replay, timing windows, or three-layout comparability was established. The original `scalesmallprocessingdevelopment` deliberately stays red at `Small Federation Processing layout remains unqualified`; Task 37 stays `[~]`. Focused `ScaleBenchmarkProfileContractTest` (`--rerun-tasks`) and strict `:neoforge-1.21.1:test check build` passed after the final testmod edit.

## Physical Bridge and target-binding continuation

- A selected red route run failed at the absent common Fabric assertion. The first physical north-facing Bridge run proved distinct native boundaries, valid common Fabric, directional EXECUTE/SUPPLY Policy, and Owned epoch-1 Endpoint Claim, then crashed on a duplicate Provider-to-storage edge: `ProviderRuntime` exposes the physical north face while the earlier fixture-created explicit edge still existed. The fixture now destroys only that non-in-world edge and verifies the physical north reconnection. An intermediate one-shot push was denied with `FABRIC_DISCONNECTED`: reconnecting the source edge invalidated the Bridge publication until its lifecycle refresh.
- The final development route uses red source cable `(4,2,1)` and blue target cable `(5,2,1)` with an east-facing physical Bridge, all inside x/z 1..15. After the source-edge handoff, a Bridge neighbor refresh republishes a shared Fabric and reactivates the Policy. Production `ProviderRuntime` resolves `ACTIVE`; one native input push succeeds and one cobblestone reaches the physical target ME cell, with no fabricated return or third job. Exact positive receipts: `.omo/evidence/task-37-physical-route-east/scale-small-route.log`; selected command: `./gradlew :neoforge-1.21.1:runGameTestServer -PfederationGameTestSelection=benchmark -PfederationGameTestId=scalesmallfederationroutedevelopment -PfederationNativeEvidenceFile=$PWD/.omo/evidence/task-37-physical-route-east/benchmark-native.properties --no-configuration-cache --dependency-verification=strict` -> `All 1 required tests passed :)`, `BUILD SUCCESSFUL`, and `federationCleanupGameTest`.
- The identity-only selector passed independently, while `scalesmallprocessingdevelopment` produced the same two native one-job receipts then failed only at its deliberate `Small Federation Processing layout remains unqualified` assertion. Focused `ScaleBenchmarkProfileContractTest` with `--rerun-tasks`, strict `:neoforge-1.21.1:test check build`, Java diagnostics, and `git diff --check` passed. No owned GameTest process or world remained. The physical input route is not an authentic Federation planner/CPU/Endpoint/return job or Task 37 timing qualification; Task 37 stays `[~]`.

## Third small-layout job continuation (2026-09-23)

- The selected `scalesmallprocessingdevelopment` failure before the change was exactly the deliberate `Small Federation Processing layout remains unqualified` gate after the native-big-grid and native-subnet one-job receipts, not a target-bus or return-path failure. Its red output is `tool_0ce36feff001KkcJHk4s3BwDW3` and `.omo/evidence/task-37-third-job-red/`.
- The same selected test now runs one additional authentic CPU-submitted job through the production Provider runtime and claimed physical Endpoint, a target ME cell, a configured AE2 Export Bus facing the registered machine, the Endpoint's south-sided item return capability, the exact owning native Provider return inventory, the requester callback, and the source physical cell. The test observes one cobblestone in the target cell before machine processing, verifies exact return-owner logic/inventory and capability identity, then asserts zero remaining source and target input, one machine output, one accepted callback, one source physical diamond, an empty Provider return inventory, and an idle CPU. The route's final receipt reports `resolution=ACTIVE pushAccepted=false targetCobble=0`; `pushAccepted=false` means no test-authored direct push was used for this job.
- Final selected command: `./gradlew :neoforge-1.21.1:runGameTestServer -PfederationGameTestSelection=benchmark -PfederationGameTestId=scalesmallprocessingdevelopment -PfederationNativeEvidenceFile=$PWD/.omo/evidence/task-37-third-job-final-receipts/benchmark-native.properties --no-configuration-cache --dependency-verification=strict`. The persisted `.omo/evidence/task-37-third-job-final-receipts/scale-small-processing.log` has exactly three layout receipts with `jobs=1`, distinct `jobIds`, `finalInput=0`, `machineOutput=1`, `callback=1`, `physicalOutput=1`, and Federation `targetInputObserved=true targetFinalInput=0 endpointReturn=owner-exact`; one required GameTest and Gradle both passed. Native output: `tool_0ce441cbf001fM2BqXiAnu2mBf`; route receipts: `.omo/evidence/task-37-third-job-final-receipts/scale-small-route.log`.
- The relocated Export Bus remains a separately seeded physical part at `(6,2,4)` facing the machine at `(5,2,4)`, adjacent to the Endpoint's south logistics face. Rerunning `scalesmallfederationroutedevelopment` passed with `resolution=ACTIVE pushAccepted=true targetCobble=1` (`.omo/evidence/task-37-third-job-route-regression/`); rerunning `scalesmallfederationidentitydevelopment` passed with settled separate target lineage (`.omo/evidence/task-37-third-job-identity-regression/`). Both also passed one required GameTest and Gradle exited zero.
- This is a one-job, 16-Grid development scene only. It does not populate the tier's declared 256 logical Patterns or 1,000-10,000-unit replay; no late/ultra three-layout scenes, matched timing windows, repetitions, raw samples, reference medians, or perf-case verifier exist. The older statements above describe earlier checkpoints, not the current small development selector. Task 37 remains `[~]` and no speedup or scale support is claimed.

## 2026-09-24 manual-only native-big-grid two-Pattern probe

- The unchanged `scalesmallprocessingdevelopment` selector passed before this slice (`tool_0ceb7f553001ucfKwNngAH5pCw`): three one-job layout receipts, one required GameTest, zero Gradle exit. The new `scalenativebiggridtwopatternsdevelopment` selector first failed on the baseline one-slot fixture at `Second physical Pattern slot is empty` (`tool_0ceba5ece0018zGmjm9yepA61L`, one required failure, Gradle nonzero). An earlier red-test placement inside the one-time `succeedWhen` setup branch was incorrectly bypassed by the next retry and was moved to the repeating assertion path before the meaningful red run.
- The final selected run `./gradlew :neoforge-1.21.1:runGameTestServer -PfederationGameTestSelection=benchmark -PfederationGameTestId=scalenativebiggridtwopatternsdevelopment -PfederationNativeEvidenceFile=$PWD/.omo/evidence/task-37-two-pattern-final/benchmark-native.properties --no-configuration-cache --dependency-verification=strict` passed one required GameTest and Gradle exited zero (`tool_0cebcd873001CmKtGucM73HDWj`). The fixture decodes physical slots 0/1 as cobblestone-to-diamond and dirt-to-gold-ingot, and asserts both are advertised by the native Lane. The same physical source cell, CPU, requester, Provider return and registered machine execute two serial real planner/tracker submissions. The machine is configured with two typed recipes before input arrives; transition receipts are filtered by exact typed keys and require one accepted/consumed/produced for each. Physical input is one at each insertion, zero after completion; physical diamond remains one while the second job runs, and final diamond/gold are each one. The return inventory and CPU are idle at each completion. No output was removed between the two jobs.
- Persisted `.omo/evidence/task-37-two-pattern-final/scale-small-processing.log` records first native UUID `656be135-36bc-4bbc-accf-5cf0bce0972e` and then two distinct UUIDs including `1ae40e26-ec1d-47bd-8888-999f65a96710`; each typed receipt has `plannerComplete=true`, `finalInput=0`, `machineOutput=1`, `callback=1`, `physicalOutput=1`. Native observation sequences 1-6 show two accepted pushes and two changed return injections. The selected test success closes requester, observation, physical source and auxiliary fixture; a mid-stage assertion failure still relies on `federationCleanupGameTest` runner-finalizer cleanup, not a fixture-local `finally`.
- Regression command `./gradlew :neoforge-1.21.1:runGameTestServer -PfederationGameTestSelection=benchmark -PfederationGameTestId=scalesmallprocessingdevelopment -PfederationNativeEvidenceFile=$PWD/.omo/evidence/task-37-two-pattern-regression/benchmark-native.properties --no-configuration-cache --dependency-verification=strict` passed one required GameTest, with three one-job receipts in `.omo/evidence/task-37-two-pattern-regression/scale-small-processing.log`. Focused `./gradlew :neoforge-1.21.1:test --tests '*ScaleBenchmarkProfileContractTest' --rerun-tasks --no-configuration-cache --dependency-verification=strict` and full `./gradlew :neoforge-1.21.1:test check build --no-configuration-cache --dependency-verification=strict` passed. Both changed Java files had empty LSP diagnostics; `GIT_MASTER=1 git diff --check` passed. No owned GameTest Java process, runtime world, or `world/session.lock` remained after the finalizer.
- This is not a comparable one-Grid native-big-grid scale layout: the development scene still has auxiliary isolated Grids. It neither builds the 256-Pattern catalog nor measures 4,096 units, late/ultra tiers, timed windows/repetitions, resource-matched layout throughput, or reference medians. Task 37 remains `[~]`; Task 39 artifact evidence predates this source and needs final requalification later. No source was deployed remotely.

## 2026-09-23 remote idle-machine directory preflight

- Strict-host-key, batch-mode SSH with the user-supplied local identity entered `~/ae2f` and verified physical cwd `~/ae2f` before and after setup. The root was empty and all target names absent; its parent was listed before creation. Only `src/`, `.gradle/`, `.cache/`, `tmp/`, and `evidence/` were created there, with no source snapshot or runtime content.
- Postflight listed all five paths as empty real directories, checked each canonical target remained inside the physical root, and found no symlink in the shallow tree. The system JDK's `java` and `javac` still report 21.0.12.1 at `/usr/lib/jvm/java-21-openjdk-amd64`; no Java provisioning, Gradle, GameTest, Minecraft, or benchmark ran.
- A future isolated launch must set `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64`, `HOME=~/ae2f`, `GRADLE_USER_HOME=~/ae2f/.gradle`, `TMPDIR=~/ae2f/tmp`, `XDG_CACHE_HOME=~/ae2f/.cache`, and `JAVA_TOOL_OPTIONS` with both `-Duser.home=~/ae2f` and `-Djava.io.tmpdir=~/ae2f/tmp` before entering a verified source snapshot in `src/`. Keep game directories and evidence under that root too; Task 37 EULA requires separate approval if a remote GameTest needs it. Do not promote this directory setup to benchmark evidence.

## 2026-09-23 UTC remote source staging continuation

- With explicit EULA authorization and the revised object-only `.omo` allowance, source is staged at `~/ae2f/snapshots/task37-20260923T151304Z-932aaf10/checkout`. The sparse working tree contains no `.omo`; 44 screened committed `.omo` current-HEAD blobs exist only inside `.git` object storage. The minimal pack carries current HEAD blob payloads plus commit/tree history, not historical blob payloads; the temporary transmitted pack was removed after indexing.
- Exact HEAD `364173f51241fda172a4ff23b1fc3bc0105a214d` and 659 allowed path hashes/modes match the local capture, including eight modified tracked and 22 untracked files. Local and remote scoped status, scoped diff and Gradle-equivalent status-derived dirty digest agree. No remote Gradle, Minecraft, server or benchmark ran. The full proof, caveats and read-only repeat commands are appended to `.omo/evidence/task-37-remote-transfer/verification.md`; Task 37 remains `[~]`.

## 2026-09-24 one-source-Grid correctness slice

- The manual-only `scalenativebiggridonesourcegriddevelopment` selector reuses the existing two-Pattern/two-job AE2 probe, without `ScaleGridFixture`. A repeating live-node assertion scans the bounded scene's AE2 block entities, includes the Provider/requester managed nodes, expands discovered native Grids via `getNodes()`, and compares exact `IGrid` references. Its initial red against the old setup observed 17 Grids (15 auxiliary, source, and isolated power cell), failing specifically at the named one-Grid assertion.
- The physical creative power cell is now attached to the Provider in this selector only, with both nodes pre-tick seeded from the same source ID; the ordinary old selectors retain their isolated power and exact 16-Grid assertions. The final run observed five distinct active nodes on one settled Grid before work and after each typed job. Both physical Patterns were decoded from slots 0/1 and advertised by the native Lane; two distinct AE2 link UUIDs completed cobblestone-to-diamond and dirt-to-gold-ingot planner/CPU/machine/return paths with one per-key physical output, zero remaining input, empty Provider return and idle CPU after each job.
- Exact red, failed intermediate, final green, two-pattern and three-layout serial regression transcripts, focused JUnit, full strict build, diagnostics, and process/world/lock cleanup are documented in `.omo/evidence/task-37-one-grid-development/verification.md`. This is an untimed five-node correctness slice, not a 256-Pattern, 4,096-unit, 16-target, late/ultra or comparable measured scale claim. Task 37 remains `[~]`; the remote snapshot was not changed.

## 2026-09-24 physical 256-Pattern catalog development

- Manual selector `scalenativeonegridcatalogdevelopment` first failed at its repeating inventory assertion, exactly `2` occupied slots against `256`. The completed selector owns one 256-slot mapped Provider inventory on the verified one-Grid topology. Slots 0/1 retain cobblestone->diamond and dirt->gold ingot; slots 2-255 use deterministic disjoint vanilla registry item input/output pairs, each encoded as a real AE2 Processing Pattern with one item in and one item out. A registered capability-backed machine receives the same 256 recipe pairs, not copied craftables or synthetic returns.
- The selected green native GameTest asserted 256 occupied/decoded slots, 256 distinct input/output keys, 256 Lane details, 256 native crafting-service craftables, 256 machine recipes, and exact per-slot pattern-to-machine-to-native matching. Five scene nodes shared one settled Grid before and after two real serial jobs, with distinct native link UUIDs, per-key physical conservation, native return/CPU idle. The original one-Grid and two-pattern selectors and the three-layout development selector passed serially; focused JUnit, full strict build, Java diagnostics, diff check, and process/lock cleanup passed. Exact commands, receipt row boundaries, UUIDs, and scope are in `.omo/evidence/task-37-256-catalog-development/verification.md`.
- Catalog population is not workload replay. Only two catalog entries ran as one-unit jobs, not 256 jobs or 4,096 units; no 16-target timed or comparable three-layout profile, late/ultra support, medians, or speedup is established. Task 37 remains `[~]`; remote source snapshot unchanged.

## 2026-09-24 high physical catalog slot execution

- The manual-only one-Grid catalog selector now completes three serial authentic native jobs, using the decoded physical stack at slot 255 to select `minecraft:command_block_minecart` -> `minecraft:golden_pickaxe` after slots 0/1. The exact high-slot input/output pair is required in the Lane and native crafting-service lookup before the third planner request, and its typed capability machine transition, native return/callback, physical source result, distinct third link and retained earlier outputs are checked on the repeating `succeedWhen` path. It still asserts 256 occupied/distinct decoded Patterns, 256 native advertisements, 256 machine recipes and one settled Grid with five active nodes.
- Red at two completed jobs, green at three, serial older selector regressions, focused JUnit, strict build, diagnostics, log row/process binding and finalizer cleanup are recorded in `.omo/evidence/task-37-high-slot-development/verification.md`. These are three one-unit jobs only, not all 256 jobs or 4,096-unit replay; Task 37 remains `[~]` and the remote snapshot was not changed.

## 2026-09-24 batch-16 physical catalog development

- New manual-only `scalenativeonegridcatalogbatch16development` uses the unchanged 256-slot, 256-machine-recipe one-Grid catalog and three decoded physical slots 0/1/255. Its native planner final output and tracked requester amount are 16 per job. A selector-only testmod machine mode consumes exactly one capability-accepted input and produces one output each tick; sixteen genuine typed machine transitions, callback 16, physical output 16, empty return and idle CPU are checked separately. The high slot is explicitly command_block_minecart -> golden_pickaxe; diamond and gold_ingot remain 16 each while it runs.
- A genuine failing-first selected run with the old one-unit planner request failed at `Native Processing plan does not produce requested quantity` after the new selector had inserted 16 inputs. The final process passed one required GameTest with three distinct native UUIDs and 48 total outputs; all four preceding selectors, focused JUnit and strict full build passed serially. Exact red/green commands, final-process rows, UUIDs and cleanup are in `.omo/evidence/task-37-batch16-development/verification.md`.
- Three output types fit the physical source cell; this says nothing about 256 output types or a Drive. No 256-job/4,096-unit replay, 16-target layout, timing qualification or remote source change. Task 37 remains `[~]`.

## 2026-09-24 64-type physical Drive continuation

- `scalenativeonegridcatalog64typesdevelopment` decodes slots 0..62 and 255 from the real 256-Pattern inventory, then runs 64 separate native 16-output planner/tracker/CPU jobs against the original direct ME Chest callback destination. The real 16k source cell held 63 distinct outputs, and a no-Drive red run failed on insertion of the high-slot input for job 64. The initial 2400-tick run only timed out after 35 jobs; the bounded 5200-tick selector timeout was derived from that observation and the 1024 machine-tick lower bound.
- The Drive-enabled green run uses a pre-tick-seeded, physical same-Grid Drive with two actual 16k cells. Only after each complete native callback and exact typed machine/conservation check, the selector extracts the accepted 16 from the direct chest and retains them in cell `jobIndex / 63`, checking chest and each cell before/after, every prior key and mounted native Grid visibility. Final run passed one required GameTest with 64 UUIDs, 64 one-item transition sets, 64 typed callback/drain results, 64 distinct retained outputs and 1024 units on one settled Grid with six active nodes. Five original selectors, focused JUnit, strict build and cleanup passed serially. Full row/process binding and exact commands: `.omo/evidence/task-37-64-type-development/verification.md`.
- This is an untimed correctness slice, not 256 jobs/4096 units or qualified matched layouts. Task 37 remains `[~]`; remote snapshot and production remain unchanged.

## 2026-09-24 256-pattern by 16-output-unit one-Grid replay

- The manual-only `scalenativeonegridcatalog256replaydevelopment` decodes physical slots 0..255 and executes one real native 16-unit planner/tracker/CPU job per distinct Processing Pattern. With only two physical Drive cells, the selected red completed 127 jobs but could drain only 126: the 127th output occupied the direct chest at 16 while both cells rejected simulated insertion because each already held 63 output types. The same selector with five actual mounted 16k Drive cells passed exactly one required GameTest. It has 256 distinct UUIDs, 256 typed machine accepted/consumed/produced and callback/result/drain accounts, exact read-back of all prior keys, and final actual cell key-set/amount distribution `63,63,63,63,4` types and `1008,1008,1008,1008,64` units, total 4096. The fifth cell holds decoded high slot255 golden_pickaxe output at 16.
- Six older selectors, focused JUnit, strict `test check build`, Java diagnostics and process/world cleanup passed. The 23000-tick bound belongs only to this manual development selector, derived from the earlier 64-job 5200-tick bound and a 4096 machine-tick minimum, not a benchmark window. Exact red/green commands, log row boundaries and independent process counts are in `.omo/evidence/task-37-256-replay-development/verification.md`. This is still just one untimed Grid, not Task37's 16-target three-layout timed qualification; Task37 remains `[~]` and remote/production are unchanged.

## 2026-09-24 bounded one-subnet three-job batch-16 gate

- The new manual subnet selector executes decoded physical catalog slots 0,1,255 through the same one Provider/CPU/requester with a separate real Interface, ME Chest, Export Bus and registered machine. Each job first fills the actual target cell with 16 before its bus filter is enabled, then verifies sixteen typed machine transitions, exact Provider return ownership, retired callback, empty target/source input and post-callback retention in five mounted source Drive cells. The selected final run reports three distinct UUIDs, 48 units and `3,0,0,0,0` per-cell types, with distinct source/target Grid identities. A matched one-Grid three-job five-cell control and serial 256/64/3x16/small regressions passed. The original Drive position touched and merged the subnet Interface; relocating only this selector's Drive established separation and exposed the meaningful old one-slot red. See `.omo/evidence/task-37-subnet-batch16-development/verification.md`. This does not establish a Federation job, multi-target throughput, timing, or Task37 closure.

## 2026-09-24 bounded physical Federation catalog batch-16 gate

- The new manual-only `scalefederationcatalogbatch16development` constructs one source Grid (Provider, native CPU/requester and five mounted Drive cells) and a separate physical target Grid with Endpoint, Export Bus, Bridge/Fabric, Claim, Policy and ProviderRuntime. The three decoded slots 0/1/255 each route 16 inputs into the target physical cell before bus activation, execute 16 one-item capability machine transitions through the current exact Endpoint/Lane/Provider return owner, accept 16 callback outputs, then drain to the source Drive. The final process has three distinct native UUIDs and `3,0,0,0,0` cell types / `48,0,0,0,0` units. Its route stays active and IDs settled; one-Grid and subnet matched controls, older small/route/identity tests, full 256-job one-Grid replay, focused JUnit and strict build passed. Exact red, green, process-bound receipts and cleanup: `.omo/evidence/task-37-federation-batch16-development/verification.md`. This is one untimed Federation target only; Task37 remains `[~]`.

## 2026-09-24 one-target Federation full catalog correctness replay

- Manual-only `scalefederationcatalog256replaydevelopment` expands the existing one-target physical Federation route from decoded slots 0/1/255 to every slot 0..255, one native 16-unit planner/tracker/CPU job per distinct Pattern, while preserving the older three-job selector. The selected red completed only three and failed the intended 256-job assertion; a subsequent 23,000-tick run completed 149 authentic jobs before timing out. The justified 46,000-tick new-selector-only run passed 256 unique UUIDs, 256 pre-export target input arrivals, 256 typed machine/callback/Drive accounts, exact authorized Endpoint Lane/return owner, and five actual cell distributions `63,63,63,63,4` types / `1008,1008,1008,1008,64` units. The high-slot golden pickaxe sits in cell 4. Older Federation/subnet/one-Grid controls, focused JUnit, strict build, diagnostics and world cleanup passed. Commands and process-bound row ranges: `.omo/evidence/task-37-federation-256-replay-development/verification.md`. This is one untimed target, not a 16-target or measured Task37 qualification; `[~]` remains.

## 2026-09-24 one-target native-subnet full catalog correctness replay

- Manual-only `scalenativesubnetcatalog256replaydevelopment` is the native-subnet counterpart: red after three authentic decoded slot 0/1/255 jobs at the explicit 256-job count assertion, then green after selecting the complete physical 0..255 catalog. The final single process has 256 separate native planner/CPU/requester links, 256 pre-export physical target-cell arrivals of 16, 256 per-key 16-transition machine/callback/Drive accounts, 256 distinct native link UUIDs and 4,096 retained units in five actual Drive cells (`63,63,63,63,4` types). The target's physical Interface/ME Chest/Export Bus are seeded to one confirmed target lineage distinct from the confirmed source CPU/requester/Drive Grid, only in this new selector. The old three-job subnet selector, one-Grid 256 and Federation 256 passed serially; strict build, diagnostics, diff check and world/lock cleanup passed. See `.omo/evidence/task-37-subnet-256-replay-development/verification.md` for failed intermediate attempts and process-bound receipts. Only one working subnet target is proven. No late/ultra measured profile or 16-target comparison is established; Task 37 remains `[~]`.

## 2026-09-24 two-target native-subnet correctness continuation

- New manual-only `scalenativesubnettwotargetsdevelopment` uses one settled source Provider/CPU/requester/Drive Grid with one shared two-slot encoded Pattern inventory and exactly two disjoint AE2 provider media. Lane 0 EAST runs cobblestone-to-diamond on physical target A; Lane 1 SOUTH runs dirt-to-gold-ingot on physical target B, each with its own Interface/ME Chest/cell/Export Bus/machine Grid and settled ID. The failure-first east/east process completed A then observed B's dirt in A rather than B. With south routing, both separate target cells each held their own 16 inputs before bus activation, then sixteen typed one-item transitions through distinct machine/return owners, two distinct native links, exact per-key callbacks and physical source/Drive retention of 32 units passed. The older three-job and 256-job subnet selectors passed serially. Exact process logs, commands, LSP/build gates and world/lock cleanup: `.omo/evidence/task-37-native-two-target-development/verification.md`.
- This expands the correctness boundary from one to two **serially** working native subnet targets, not concurrent throughput or a 16-target topology. The separate Federation two-target route is untouched. No late/ultra or quiesced matched 405+ minute benchmark was run; Task 37 remains `[~]`.

## 2026-09-24 two-target physical Federation correctness continuation

- New manual-only `scalefederationtwotargetsdevelopment` uses one settled source Grid for Provider/CPU/requester/Drive, one two-slot physical Pattern inventory, one `ProviderRuntime` indexing native Lanes 0/1, and two separate Endpoint/Chest/cell/Export Bus/machine target Grids. Two physical Bridge/Fabric legs, directional EXECUTE/SUPPLY Policies and separately owned Endpoint Claims keep both routes active. The second Bridge reaches the source Grid through a staged elevated red cable path; the first low south path was isolated and could not activate the leg.
- A deliberate Lane 1-to-Endpoint A binding passed the first authentic 16-output job then failed because the authorized resolution pointed at A while target B's cell remained empty. Restoring indexed binding passed two serial jobs: target A received 16 cobblestone, target B 16 dirt before each Export Bus activation, each own machine/return owner delivered 16 physical source outputs and callback units, two distinct native UUIDs retired, both source and target inputs cleared, and the source Drive retained 16 diamonds plus 16 gold ingots. Final test, older three-job and 256-job one-target Federation controls, and two-target native-subnet control each passed one selected required GameTest in separate processes. Strict `test check build`, four Java diagnostics, diff and process/world-lock checks passed. Full process-bound receipts and commands: `.omo/evidence/task-37-federation-two-target-development/verification.md`.
- This supersedes only the previous statement above that a Federation two-target route was untouched. The new proof is **serial two-target correctness**, not 16 targets, concurrent throughput, two-target 256-job replay, late/ultra timed tiers, resource parity, reference medians or speedup. Production routing was unchanged, the remote snapshot was not updated, and Task 37 remains `[~]`.

## 2026-09-24 direct native one-Grid two-machine correctness

- Manual-only `scalenativeonegridtwomachinesdevelopment` uses one source Provider inventory with disjoint slots 0/1 and exactly two adjacent registered capability machines, EAST and SOUTH. Both Lanes, requester, CPU and mounted Drive remain on one settled native `IGrid` (six active nodes), not two Grids or auxiliary idle fixtures. Native `CraftingService.getProviders(decoded)` equals the exact mapped Lane for each slot.
- The intentional EAST/EAST red completed A's 16-output native job and physical Drive drain before B's real Lane 1 push repeatedly rejected at A, leaving south B without work; the selected GameTest reported one required failure. Lane 1 SOUTH passed both serial 16-unit jobs with distinct native UUID, exact different physical machine and return-inventory owners, 16 one-item transitions and 16 callbacks each into the direct chest, then Drive readback of 16 diamonds and 16 gold ingots. The older one-Grid 256 replay, two-target native-subnet and two-target Federation selectors passed in separate serial processes. Strict test/check/build, diagnostics, diff and owned world/process cleanup passed. Exact process-bound red/green receipts: `.omo/evidence/task-37-one-grid-two-machines-development/verification.md`.
- This advances the direct native correctness boundary only. No two-machine 256-job replay, 16-target scene, simultaneous throughput, late/ultra timed three-layout comparison, measured parity or quiesced 405+ minute reference was produced. Task 37 remains `[~]`.

## 2026-09-24 direct native four-machine one-Grid development

- Manual-only `scalenativeonegridfourmachinesdevelopment` first failed on the live two-Lane fixture with `Expected Four distinct native physical Lane faces are required to be 4, but was 2`. The final selected process placed distinct capability-backed machines immediately EAST/SOUTH/UP/DOWN of one Provider; all four sided handlers, BlockEntity identities and native return owners differ, each decoded disjoint typed Pattern is published by exactly its assigned Lane, and Provider/CPU/requester/one-cell Drive stay on one exact confirmed six-node Grid. Four serial native 16-unit jobs have four distinct UUIDs, exactly 16 typed one-item machine transitions/callback units each, zero unselected machine transition growth, and 64 physically retained Drive units. UP/DOWN required no source-power relocation. Older direct two-machine 256 replay, two-machine two-job and one-machine 256 replay controls, focused JUnit and strict full build passed serially. Process-scoped logs and commands: `.omo/evidence/task-37-one-grid-four-machines-development/verification.md`.
- This is a four-machine correctness increment, not a scale benchmark, sixteen targets, concurrent throughput, late/ultra qualification or timing reference. Task 37 remains `[~]`; production, profile manifests, remote snapshot and existing 256 catalog behavior are unchanged.

## 2026-09-24 direct four-machine 256-job one-Grid continuation

- New manual-only `scalenativeonegridfourmachines256replaydevelopment` first failed at the actual four-slot Provider inventory against a 256-slot physical assertion. Its final selected process checks all 256 decoded disjoint physical Patterns assigned by `slot % 4` to four distinct EAST/SOUTH/UP/DOWN Lane/handler/return/machine identities on one exact settled six-node Grid. Four physical machines each have 64 exclusive recipes and execute 64 serial authentic 16-unit native planner/CPU/requester jobs. A process-local audit confirms 256 distinct growing native UUID sets, 256 exact typed 16-transition/callback accounts, no unselected machine transition growth per job, and 256 types / 4096 units read back from five mounted Drive cells (`63,63,63,63,4` types). The unchanged four-job selector and two-machine 256 replay pass in separate serial processes, as do focused JUnit and strict `test check build`. Full red/green/controls, cleanup and limitations: `.omo/evidence/task-37-one-grid-four-machines-256-development/verification.md`.
- This remains one untimed direct Grid with four serially working destinations. It establishes neither 16 physical targets nor simultaneous throughput, three matched layouts, late/ultra timing windows, resource parity, reference medians or support claims. Task 37 stays `[~]`; no production, benchmark profile, manifest, plan or remote change was made.

## 2026-09-24 three physical native-subnet targets

- Manual-only `scalenativesubnetthreetargetsdevelopment` adds a second physical Provider host H1 with one encoded slot and a real in-world WEST native cable edge on H0's source Grid. H0 has two encoded slots/Lanes; A/B/C each own a separate confirmed native Grid with Interface, ME Chest/cell, power, Export Bus and registered machine. The candidate elevated cable geometry settled segment by segment from source chest `(3,2,2)` through `(3,3,2)` to `(8,2,3)`; `(3,3,2)` is a BE-free template barrier, not a source-device collision. H1 at `(9,2,3)` was pre-tick seeded from the confirmed source ID, without local WEST power, and joined through its physical WEST edge. An H1-to-A host misbinding was rejected when the first third-job input reached A; restored topology completed three serial 16-output typed planner/tracker/CPU jobs through distinct machines and return owners into the source Drive (48 units). The older two-target subnet 256 replay and direct four-machine 256 replay controls, focused JUnit, strict build and cleanup passed. Process-bound evidence: `.omo/evidence/task-37-native-three-target-development/verification.md`.
- This advances only untimed native-subnet serial correctness to three physical targets. It does not qualify three layouts, 16 working targets, concurrent throughput, late/ultra, timing baselines or Task 37 completion. Task 37 remains `[~]`; production, manifest, profiles, plan and remote snapshot were untouched.

## 2026-09-24 H1 source-power fixture repair

- A repeating managed-node NBT snapshot around H1 creation failed with `blockSame=true nodeSame=true stateSame=false`: H1's non-isolated remote setup reseeded H0's live creative energy node at the shared `ENERGY_POS` despite placing no H1 cell. The `NativeProviderSetup.create` energy seed now excludes remote hosts while local seeding and H1's own Provider seed remain. The real H1 WEST in-world edge assertion stays intact. Selected three-target rerun passed three serial 16-output native jobs on four distinct confirmed Grids; both 256-job subnet and one-Grid controls independently passed with 4096 units. Focused JUnit, strict build, diagnostics and cleanup passed. Superseding process-bound red/green/control evidence is appended to `.omo/evidence/task-37-native-three-target-development/verification.md`; Task 37 remains `[~]`.

## 2026-09-24 physical Federation three-target correctness

- A new exact manual-only `scalefederationthreetargetsdevelopment` selector first failed on the missing physical C red leg after the original two-target route became active. With H1's real WEST source cable and separate runtime/identity, staged C Endpoint/anchor/bus/machine, third physical Bridge/Fabric, H1 Claim and directional Policy, the final selected GameTest passed one required test. A deliberate absent-C-Policy native push was denied `POLICY_DENIED` before C input/export, then restored; three authentic serial 16-output planner/CPU/Endpoint/machine/return/callback jobs reached only A/B/C respectively and retained 48 typed units in the source Drive. The original two-target Federation 256 replay separately passed one required test and retained 4096 units; focused JUnit and strict build passed. Exact process-bound commands, source hashes and limitations: `.omo/evidence/task-37-federation-three-target-development/verification.md`. This is not timing, concurrency, 16 targets or Task 37 completion; `[~]` stays.

## 2026-09-24 physical Federation three-target 256 replay

- Exact manual selector `scalefederationthreetargets256replaydevelopment` split 256 decoded disjoint Pattern slots between physical H0 (171 A/B) and H1 (85 C), with exclusive `slot % 3` native publications on the existing A/B/C Bridge+H1 WEST topology. A source-bound red observed H1's old one-slot inventory; a wrong-owner modulo-3 negative saw 16 inputs in B while A was selected. The final selected process and separate three-job/two-target-256 controls passed; 256 unique native UUIDs, exact 16-unit machine/callback/physical cell accounts and actual five-cell Drive retention of 4096 units were audited. The 210,000-tick selector-only budget follows an observed 95-job timeout at 70,000 ticks. See `.omo/evidence/task-37-federation-three-target-256-development/verification.md` for commands, hashes, receipts and limitations. This is untimed serial correctness only: 16 targets, concurrent throughput, matched layouts, late/ultra tiers and stable reference medians remain missing. Task 37 stays `[~]`, F1 rejected.

## 2026-09-24 high-slot authority superseding correction

- The preceding green was superseded after detecting gratuitous B HandlerAuthority for slot 255 (`255 % 3 == 0`, A-owned). `MixedFactoryObservation.authorizeMachine()` accumulates entries, so physical recipe and output correctness alone did not reject this invisible extra. A repeating probe assertion failed first at the exact owner mismatch; the high-slot three-target branch now selects A machine/Provider, while direct-four and two-target branches are unchanged. Final selected 256 replay and independent three-job, two-target 256 and direct-four 256 controls passed with strict build/JUnit and a rebased 4096-unit process-local audit. Exact source hashes, exits and limitations: `.omo/evidence/task-37-federation-three-target-256-development/verification.md`. Still untimed serial development correctness; Task 37 `[~]`, F1 REJECT.

## 2026-09-24 remote fixed-snapshot development verification

- The immutable 665-file snapshot `task37-20260924T002103Z-5a12fe02dbe7` retained canonical manifest digest `5a12fe02dbe78f63c1d64610ec7d3cbde76f84ad5b304c71c8943a120a28b30b` and corrected probe SHA `6f1949e10efc57e80534f49695e5ad991210a7ca9b71651b3bd825c871f6ed8e` before and after three serial remote selectors. Each explicit-exit run passed one required GameTest, build exit 0, 256 native jobs and actual 4096 Drive units. Direct four-machine, native-subnet two-target and Federation three-target process-local audits confirmed distinct UUIDs, exact target splits and five-cell readback; task-owned GameTest Java children had observed affinity 2-9. No EULA file or world lock remained and no source was edited. Exact commands, receipts and limitations: `.omo/evidence/task-37-remote-development/verification.md`. This remote correctness characterization does not resolve unmatched target/resource budgets or timing qualification; Task 37 `[~]`, F1 REJECT.

## 2026-09-24 fourth physical Federation serial development

- New manual-only `scalefederationfourtargetsdevelopment` leaves the three-target selector intact. H2 uses an extended red source trunk and its physical WEST Provider edge; an independent red/blue Bridge leg reaches D's own anchor/Endpoint/Export Bus/machine Grid. Missing-D topology and, only after three authentic A/B/C jobs, missing-D native-job processes both failed explicitly. Final process passed four serial 16-unit native planner/CPU/requester jobs, distinct links and return owners, exact target input/typed machine/callback accounts, and physical source Drive readback of 64 units. Old Federation-three, native-subnet-four and Federation-three 256 replay passed serially; focused JUnit, strict build, LSP and cleanup passed. Exact hashes, process-bound log cardinalities and constraints: `.omo/evidence/task-37-federation-four-target-development/verification.md`. No 16-target, timing or three-layout qualification; Task 37 `[~]`, remote snapshot unchanged.

## 2026-09-24 four physical native-subnet 256-job replay

- Separate manual-only `scalenativesubnetfourtargets256replaydevelopment` passes all 256 decoded modulo-four physical Pattern jobs, 64 serial real 16-unit jobs on each of A/B/C/D, distinct 256 native links, 256 unique typed keys, exclusive physical machine/return owners, and 4096 units in five source Drive cells (`63,63,63,63,4` types). After live D topology, the first run failed at D's old one-slot inventory; a machine reconfiguration intermediate was corrected before the final green. Direct-four 256, original native-subnet-four four-job, and Federation-four 256 controls passed serially, as did strict build and process-local audit. Exact commands/hashes and cleanup: `.omo/evidence/task-37-native-four-target-256-development/verification.md`. This remains untimed correctness, not a 16-target scene or Task 37 completion; `[~]` stays.

## 2026-09-24 large empty structure preflight

- Separate manual-only `scalelargestructurepreflightdevelopment` loads the genuine empty 36x8x36 template and passes placed bounds, 482 enumerated source/route/pod positions, and actual candidate/full placed chunk recording without constructing targets. Selected old 3x3x3 bounds and occupied chest BlockEntity controls each fail on their intended invariant in independent serial processes. The pinned framework still owns temporary forced chunk tickets; no product or fixture ticket was added. Exact commands, source/NBT/log hashes, earlier malformed-NBT correction, strict build and cleanup are in `.omo/evidence/task-37-large-structure-preflight/verification.md`. This does not qualify a 16-target topology, late/ultra three-layout profile or measured speedup. Task 37 stays `[~]`.

## 2026-09-24 sixteen physical source hosts

- Separate selected `scalesixteensourcehostsdevelopment` on the same empty structure passed 16 independently placed Pattern Provider BEs and registered Lanes after a meaningful red at 15 hosts. The one settled source Grid retains one CPU/requester/Drive, an online native controller, 154 staged dense/ordinary source cables and sixteen real WEST edges, one channel per Provider. The runtime dense root carries 16 and Grid-wide pathing reports 19 under unmodified DEFAULT mode; each physical edge and cable bottleneck is checked. All 176 distinct placement positions were bounds/loaded/BE-free guarded before placement. Strict build, LSP, process/world cleanup and exact source/log hashes: `.omo/evidence/task-37-sixteen-source-host-development/verification.md`. No sixteen working target machines/Grids, Federation/Interface routing, throughput or timed three-layout qualification follows. Task 37 remains `[~]`.

## 2026-09-24 large-source direct one-target Processing

- New manual-only `scalelargedirectonetargetdevelopment` retains all 16 independently channelled source Provider hosts but installs one capability-backed direct machine only at host 0 EAST. The missing-machine red ran after all 16 channels settled. The final selected process decoded host 0's one physical cobblestone-to-diamond Pattern, submitted one real native planner/CPU/requester UUID for 16 outputs, observed 16 one-item machine/return-owner transitions and callback units, and read back 16 diamonds from the original source Drive. The other 15 Lanes remained unpublished. Original source-only and direct-four selectors passed serially, as did strict build, diagnostics and cleanup. Exact commands/hashes and the optional native-properties absence: `.omo/evidence/task-37-large-direct-one-target-development/verification.md`.
- This proves just one serial direct target on the larger source, not 16 target machines/Grids, matched layouts, concurrent throughput, measured timing, late/ultra support or Task 37 completion. Task 37 stays `[~]`; no production, registered benchmark profile or remote snapshot changed.

## 2026-09-24 large-source direct two-target correctness

- The new independent manual-only `scalelargedirecttwotargetsdevelopment` selector leaves both the original source-only and one-target selectors intact. It places distinct actual capability machines EAST of physical source hosts 0/1 after checking both sites and neighbors. Each host has its own exclusive decoded slot-0 Pattern, active channel, native Lane and return owner on the same confirmed 16-Provider source Grid. The selected missing-B red was followed by two authentic serial native 16-output jobs, distinct UUIDs, exact typed machine/return/callback accounts and both output keys physically retained in the original one-cell Drive at 32 total. Both older selectors independently passed; strict build, diagnostics and process/world cleanup passed. Exact process-bound commands/hashes and an exploratory retry-collision caveat: `.omo/evidence/task-37-large-direct-two-target-development/verification.md`.
- This proves two direct machines sharing one source Grid, not two target Grids or all 16 machines, timed small/late/ultra layout equivalence, concurrency, speedup or Task 37 qualification. Task 37 and Final Wave remain `[~]`; no profile, production or remote snapshot changed.

## 2026-09-24 sixteen physical direct destinations

- A separate manual-only `scalelargedirectsixteentargetsdevelopment` selector now places sixteen distinct capability-backed machines EAST of the existing sixteen Provider BEs after a complete bounded/loaded/BE-free neighbor preflight. Each physical host owns its own slot-0 decoded disjoint Processing Pattern from catalog indices 0..15, unique active native Lane/return owner and exact one-Pattern service publication on the same confirmed source Grid. The selected missing-host-15-machine red occurred after all Provider channels settled. The final process passed sixteen real serial 16-output native planner/CPU/requester jobs with sixteen distinct UUIDs, typed exclusive machine/return/callback receipts, zero remaining input/return and exact 16-key/256-unit readback from the one mounted source Drive cell. Source-only, one-/two-target and direct-four selectors remained independently green; strict build, diagnostics and cleanup passed. Exact commands, process/source hashes and row boundaries: `.omo/evidence/task-37-large-direct-sixteen-target-development/verification.md`.
- The direct scene does not establish sixteen separate native-subnet or Federation target Grids, concurrency, matched resource budgets, small/late/ultra measured repetitions or reference medians. Task 37, 38, 40 and Final Wave stay `[~]`; no production/profile/remote snapshot was modified.

## 2026-09-24 sixteen physical native-subnet destinations

- Manual-only `scalelargenativesubnetsixteentargetsdevelopment` reuses the settled sixteen-channel source and constructs sixteen physical Interface/Chest/cell/power/Export Bus/machine pods on sixteen distinct confirmed target Grids. A deliberate fifteen-pod red failed at the exact missing-sixteenth assertion after source settlement; the final selected process passed sixteen authentic serial native 16-output planner/CPU/requester jobs. Each target cell held its own sixteen inputs before its bus filter was enabled, each machine produced sixteen typed one-item transitions, and distinct native links/callbacks returned the sixteen outputs to one mounted source Drive cell for 16 types / 256 units. Source-only, direct-16 and earlier four-subnet controls plus strict `test check build` passed serially. Exact commands, transcript/source hashes and resource limits: `.omo/evidence/task-37-large-subnet-sixteen-target-development/verification.md`.
- Relative to direct-16, the subnet scene adds sixteen separate target Grids, Interfaces, Chests/1k cells, creative energy cells and Export Buses. The bounded preflight checks loaded pod positions, not comparable live chunk counts; no energy-demand or peak-resource samples exist. The 2000-tick per-job wait is a correctness timeout, not a throughput observation. Sixteen-target Federation, 256-job comparable layouts, concurrency, 300/600-second repetitions and reference medians remain unproven. Tasks 37/38/40 and Final Wave remain `[~]`.

## 2026-09-24 sixteen physical Federation target correctness

- Separate manual-only `scalelargefederationsixteentargetsdevelopment` on the same sixteen-Provider source first failed the deliberate fifteen-target after-settlement count with an explicit server exception and Gradle nonzero. The repaired sixteen-target run proved 17 distinct confirmed native Grids, active physical Bridge/Fabric/Policy/Claim/Endpoint paths, and sixteen serial 16-output native planner/CPU/Provider/return/machine jobs with distinct UUIDs and exact typed transitions/callbacks. The original mounted source Drive read back sixteen types/256 units. Source-only, direct-16, subnet-16 and earlier Federation-four controls passed in independent serial processes; the strict root build passed. Full red/green/control transcript hashes, source hashes, commands and per-job row boundaries: `.omo/evidence/task-37-large-federation-sixteen-target-development/verification.md`.
- This closes the *sixteen working physical Federation targets* serial correctness gap only. Its extra Bridges, Policies, Endpoints, Grids, target cells and creative energy sources are not a matched resource budget against direct/subnet; the independent processes have different placements and no comparable live loaded-chunk, energy or peak usage samples. Neither sixteen concurrent jobs nor 256-Pattern/4096-unit comparable three-layout replay, 300/600-second repeated late/ultra windows, stable medians or negative performance cases follows. Task 37/38/40 and F1-F4 remain `[~]`; production, registered profiles and remote snapshot were unchanged.

## 2026-09-24 large direct sixteen-host 256 replay

- The source fixture's earlier one-slot capacity was a constructor setting for its mapped physical Provider inventory, not a hard 16-host topology ceiling. A new selector configures 16 encoded slots per physical Provider and five mounted Drive cells without altering old selector defaults. It passed 256 exclusive decoded Pattern/Lane/machine checks and 256 serial real native jobs with distinct UUIDs, 4096 one-item machine transitions, callbacks and physically read-back Drive units. Failure-first after source settlement rejected missing global Pattern 255 at 255 craftables. Independent source and old direct controls plus strict build passed. Observed resources and exact receipts: `.omo/evidence/task-37-large-direct-256-development/verification.md`. The selected footprint includes extra cells and 256 recipes; energy demand, total loaded chunks and comparable timings remain unmeasured. Task 37 stays `[~]`.

## 2026-09-24 reserved local host eligibility preflight

- The approximately nine-hour owner reservation permits work but does not qualify a timing host. Before sampling, `.omo/evidence/task-37-host-preflight/verification.md` fixed a seven-point 60-second load/CPU/process threshold plus actual exclusive JVM/server CPU affinity and fixed-governor requirements. Local Icelake KVM 60-vCPU host has Java 21.0.12.1 and all inspected shell/other-Java masks `0-59`; no actual Task 37 JVM pin or readable governor is established. Five samples failed the runnable cap (3 > 2), despite 1.300%-2.424% aggregate busy between samples. Several competing process names persisted across both endpoints; their exact interval CPU was not collected. Decision `NOT_YET_ELIGIBLE`, no benchmark/median and no scale qualification. Tasks 37/38/40/F1-F4 remain `[~]`.

## 2026-09-24 small replay resource-integrity preflight

- `tools/task37_resource_preflight.py` reads the three independent selected process logs and pairs all 256 ordered physical work/Drive rows; common ordered catalog SHA-256 `855432a96c9059abd13e86d6742d57fc56fb17c361d5d4c563a17419e8b2c25f`, 256 distinct native UUIDs per run, exact 4096 callback/Drive units, source Grid 19 used channels, one asserted CPU and five physical Drive cells. It separates one direct source Grid from sixteen additional Interface or Bridge/Endpoint target Grids. Current logs have no `AE2F_SCALE_RESOURCE_SNAPSHOT` with measured capacity, finite energy debit and whole-world chunk/ticket demand. The preflight exits 1 with `NON_COMPARABLE`; negative comparator tests reject changed capacity/CPU/input and missing energy/chunks, and a missing process transcript fails closed. No `perf.*` registration or timing was added. Exact commands/exits/hashes and caveats: `.omo/evidence/task-37-resource-integrity-preflight/verification.md`. Tasks 37/38/40/F1-F4 remain `[~]`.

## 2026-09-24 subsequent perf-case wiring (partial Task 37)

- The prior preflight checkpoint above is superseded only as to case registration: `perf.resource-integrity` and `perf.reject-incomparable-speedup` now dispatch through an explicit path/SHA-bound selected-input backend in `federationVerify`. The real three-log result is still `NON_COMPARABLE`, outer and validator exit 1, schema-v3 `BLOCKED`; the consumer rejects it. Eight unit tests include a **test-only** fully snapshot/receipt-bound comparable control and deliberately mismatched-capacity rejection; no invented observations enter production evidence. Missing or stale selected logs reject before semantic consumption. Exact command, attempt and hashes: `.omo/evidence/task-37-perf-verify-wiring/verification.md`. No timed profile or speedup claim; Tasks 37/38/40/F1–F4 remain `[~]`.

## 2026-09-24 first true native small timing boundary

- An accelerated GameTest clock advanced about 300000 ticks in five wall minutes, so a 300000-tick selector limit gave a genuine 303-second warmup but no sample and a nonzero Gradle exit. The final selected limit is 2000000 ticks; it does not relax the real wall window. Repeated 256-Pattern cycles require explicit physical Drive drain after each exact five-cell 4096-unit assertion, while preserving global UUID/callback counts. The timed completion ledger must report **drained plus still-retained** Drive work, not call all cumulative callbacks retained units. A failing-first tier-contract test also showed that `small.json` cannot accept generic manifest lifecycle fields. Full red/intermediate/final processes: `.omo/evidence/task-37-small-timed-first/verification.md`.
- Three serial direct windows passed on the intermediate 741-file source snapshot but its completion Drive labels were wrong. The corrected final-source snapshot `be10d7f4422cce510824bbd9f678a0b25dd337aa00d3bb39d15426dc6e610a42` passed one direct repetition through `federationBenchmark` and its nested native GameTest. Warmup was 300.016450434s/81776 units and sample 602.340710402s/151616 units (251.711 units/wall second; 0.238776 units/tick). Process-local audit observed 14587 distinct UUIDs and Drive receipts, 56 complete 4096-unit cycles, 229376 drained plus 4016 final retained = 233392 callbacks. Independent persisted schema-v3 consumer, pre/post manifest, focused tests and owned cleanup passed. It does not measure subnet/Federation or finite energy/chunk/capacity equality; no ratio, strict speedup, full Task 37 or final-wave approval follows.

## 2026-09-24 authentic small subnet success and Federation work-rate failure

- A 2000000-tick ceiling initially invalidated a true subnet warmup after 300.017318747s/4278 jobs. Raising only subnet/Federation timed selectors to 5000000 ticks left wall windows and native work invariant. On immutable 742-file snapshot digest `089cd6f5cbaa0e0b041fdd184f0ac95c0a4925a9c22f351a0684ae0b9ff5e0ec`, the subnet GameTest passed a 302.338772894s/3883-job warmup and 603.215400760s/8263-job sample; 132208 sample units/603.215s = 219.172 units/s. Exactly 12146 UUIDs and Drive receipts reconcile 194336 callbacks to 192512 drained + 1824 final retained. Producer and persisted consumer passed, pre/post source hash matched, task-owned world was removed. Result SHA-256 `88f9a3f0df5f3c4aac766fa0c744923cc12fb7521afcf4eba1e71f764f30a574`.
- On that **same** source, the Federation selector completed only 27 distinct routed native jobs/432 Drive/callback units in ~300s. `ScaleTimedWindow` rejected an underworked warmup, no sample/result was emitted, and its GameTest/runner exited 1 with owned cleanup. The failure is measured authentic workload throughput, not an absent bridge/Endpoint route and not eligible timing. Direct is 0/3, subnet 1/3, Federation 0/3 for this digest. All target/source topology, finite energy, whole-world chunks/tickets, capacity and peak-demand parity remain non-comparable. Exact paths/hashes and local gates are in `.omo/evidence/task-37-small-timed-first/verification.md`; Task 37/38/40/F1-F4 remain `[~]`.

## 2026-09-24 final source-matched two-layout continuation

- The earlier `0/3 direct, 1/3 subnet` entry was a chronological checkpoint. On **the same** immutable `089cd6f5...` source, five further serial processes completed: three direct, two subnet. All nested GameTests, outer Gradle producers and independent persisted consumers exited zero, pre/post 742-file source manifests matched, worlds were absent between runs, and `aggregate.receipt` exited zero. Job count = unique native UUID count = physical Drive receipt count per process: direct **14589/14858/15104**, subnet **12146/12200/12799**. Each native completion reconciles `callbackUnits = drainedDriveUnits + finalDriveUnits`. Direct sample units/wall-second were 252.791/269.114/268.419 (median **268.419**); subnet 219.172/223.831/231.325 (median **223.831**). The **1.199x** ratio of raw median rates is descriptive only, never a strict comparable speedup. Source-specific status is now **direct 3/3, subnet 3/3, Federation 0/3** because the failed Federation warmup has no accepted phase/sample/result. Missing finite energy/capacity/whole-world chunk and ticket/peak-demand pairing continues to block strict comparison and Task 37/38/40/F1-F4 qualification. Full per-attempt paths, hashes, phase ticks/wall work and caveats: `.omo/evidence/task-37-small-timed-first/verification.md`.
