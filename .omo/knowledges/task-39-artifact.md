# Task 39 artifact qualification handoff

## Baseline (2026-09-23)

The unchanged `./gradlew :neoforge-1.21.1:check :neoforge-1.21.1:build :neoforge-1.21.1:verifySharedJarContent --dependency-verification=strict --no-configuration-cache --no-daemon` passed. The baseline binary, sources and lock metadata SHA-256 values were respectively `b3eda0e8863a7818472c75ffcf2020e303effcc9c6669830e753f0cf559ccb7c`, `803c3bea3bb04c8c58f40472022dc21b519ccdfd66f7930fdd54e1e14a0d713d`, and `2677d763489033fdd42a50b0c523cf1bb7d12b158fe71752a394b8d14b382b1b`. The baseline binary has 444 ZIP entries. `META-INF/MANIFEST.MF` states MC 1.21.1, NeoForge 21.1.250, AE2 19.2.17 and LDLib2 2.2.34. `META-INF/neoforge.mods.toml` has exact singleton version ranges for those four runtime dependencies and AGPL-3.0-only. These are observations of the existing local archive, not a claim of a clean rebuild or a runtime-loaded JAR.

The same command without `--no-configuration-cache` fails while deserializing the existing `verifyReleaseArchive(File, boolean)` closure (`Class 'boolean' not found in Gradle runtime class loader`). Keep this distinct from archive semantics and do not claim the default configuration-cache command passed.

## Shared-file integration blocker

`./gradlew :neoforge-1.21.1:federationVerify -Pcases=artifact.clean-build,artifact.same-jar-client-server,artifact.license-manifest,artifact.reject-wrong-tuple,artifact.no-extra-target -PevidenceDir=.omo/evidence/task-39 --dependency-verification=strict --no-configuration-cache --no-daemon` fails at `gradle/federation-qa.gradle:6514` with every Task 39 case unknown. `tests/scenarios/manifest.json` has no artifact cases; `executeHarness` in the shared QA file hard-codes per-case dispatch, result assertions and persisted semantic verification. Adding a manifest alone could produce no executable work. Task 37 is simultaneously editing benchmark-specific files, and shared QA changes are explicitly forbidden during its ownership. Stop before touching that file and notify Atlas; serialize the later Task 39 integration and real client/GameTest launches.

## Required closure after ownership transfers

- Add failing-first negative tuple and corrupted/split-JAR identity tests, then wire exact five-case production and persisted evidence dispatch through the shared owner. Preserve the existing schema-v3 source revision plus dirty bytes, dependency lock and fresh attempt binding. An old attempt, dirty untracked source or rehashed forged log must fail.
- Use a task-owned disposable checkout and isolated `GRADLE_USER_HOME`, not `git clean` or the user's global cache. Strictly rebuild and compare reproducibility only under explicitly identical inputs; record checkout state, effective pins, JAR and lock SHA-256 and packaged entry inventory. Do not label a locally cached incremental build as fresh.
- Launch the actual same final JAR on a dedicated server and actual client, with process-observed JAR SHA-256 in both logs and an authentic client-to-server acknowledgment. Reject child failure even if nested Gradle exits zero or prints a success banner. Bound execution and shutdown separately and verify no descendants, worlds, sockets or disposable cache survive.
- Inspect production binary and sources archives for testmod, optional addons and debug assets; preserve exact MC 1.21.1 tuple and do not promote Task 36's GTCEu startup-blocked or license-blocked row. Probe interrupted launch, hang, flaky replay, wrong tuple, corrupted JAR and stale evidence. Malformed user input and prompt injection are inapplicable to a fixed local artifact verification CLI; path arguments and evidence input still need normal boundary validation.

## Cleanup receipt

Preflight created no disposable checkout/cache/process/port and no Task 39 attempt; nothing external was removed. It did use the existing workspace and Gradle cache for baseline characterization, so that run cannot be cited as the required clean-environment qualification. No Task 39 manifest, workflow or product source was modified, and the plan checkbox remains open.

## Independent implementation continuation (2026-09-23)

The preflight-only state above was superseded by an independent artifact implementation. `tools/artifact_verify.py` inspects both archives and snapshots tracked/untracked regular source into a task-owned detached Git worktree with an empty temporary Gradle home. It executes strict `check build`, compares binary and sources archive SHA-256 against the local just-built archives, records the complete packaged entry lists, source digest, revision and dependency-lock hash, then removes its own worktree/cache. The accepted local comparison is `.omo/evidence/task-39-independent/clean-build.json`; its binary is `e6a284dd1cd8b5a9227f1121417d218903003ab9e2387a5134c9459cbaf7085e`, sources are `bd3457ed20c2fa170ca85a0764f52be7f2f581accf17381fd56b4968d8359760`, and source digest is `0b48e11552cd6cd876dc3fcf8bdb931f54991f5036d6e0eedac963e79ef2ed21`. This digest matched the worktree at the last check; any subsequent Task 37 writes require regeneration before claiming current evidence.

Both archives now package `META-INF/LICENSE` byte-for-byte. `docs/compatibility/artifact.json` pins the exact one-target tuple and license digest; `artifact.md` records hook provenance, licensing, reproduction and the still-unverified same-binary runtime approach. Six stdlib unit tests mutate copies of the real built archive and reject wrong tuple, corrupt ZIP, split runtime files, stale rebuild and extra target. They passed with `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p test_artifact_verify.py -v`; strict `check build` and `artifact_verify.py inspect` passed. `.github/workflows/quick.yml` runs strict JUnit/archive and required native GameTest with actual log inspection; `qualification.yml` offers manual/weekly full compatibility and isolated rebuild qualification. Neither publishes or opens PRs/issues.

One initial empty-cache build failed externally at NeoForged Mojang Meta HTTP 502. The next isolated run built but detected three empty `appeng/` directory ZIP metadata differences in sources JAR, not source bytes. Configuring `sourcesJar.includeEmptyDirs=false` made the next isolated rebuild byte-identical in both archives. The checker logs are under `.omo/evidence/task-39-independent/`. Its disposable worktree/cache were deleted, `git worktree list` shows only the original checkout, and no GameTest/client or server process was started while Task 37 owns shared runtime/QA. The exact five-case shared-QA integration, real same-binary launch, and runtime adversarial probes remain pending. The plan checkbox stays open.

## Five-case qualification (2026-09-23)

The pending integration above is now implemented. `tests/scenarios/manifest.json` registers the five `artifact.*` cases, and `gradle/federation-qa.gradle` drives a fresh detached-worktree/fresh-cache build, an installed-JAR runtime, and seven negative unit tests. Its persisted schema-v3 consumer rechecks artifacts, child lifecycle, installed archive hashes, NeoForge-observed mod paths and SHA-256, and both sides of the named client join. `runArtifactServer` and `runArtifactClient` set ModDevGradle `loadedMods=[]`, install byte-identical product archives in separate task-owned `mods` folders, and use Minecraft 1.21.1's `--quickPlayMultiplayer` rather than ignored legacy `--server`/`--port` arguments. At mod construction the optional proof reads NeoForge `ModList`'s actual mod file path and hashes the file; opt-in event listeners record server/client player acknowledgments. The client uses Xvfb on headless Linux. Killing only the Gradle wrapper process group left JavaExec descendants alive; terminating owned descendants before waiting for wrappers fixed lifecycle rejection.

Accepted result: `.omo/evidence/task-39/attempt-20260923T095329159Z/result.json`. Source revision is `364173f51241fda172a4ff23b1fc3bc0105a214d`, dirty identity `22a8ddddcde4266f2accc754c6ff68dbc7516a9ce12d12806dafaadfe4ed7c77`, dependency lock `2677d763489033fdd42a50b0c523cf1bb7d12b158fe71752a394b8d14b382b1b`, binary `94f6b657fc9ff1d90d295215ef9bebe665d920c3d8ffb96838f3647a644a69ed`, and sources `cf53534424a19db9d112ddc17e2e922d25268140e46b8eb009d8ef3527f9302d`. All five assertions and three subprocess exits passed, without timeout or surviving descendants. `federationVerifyEvidence` accepted the persisted attempt against current source; strict `check build`, seven artifact unit tests and `git diff --check` passed. Later source edits invalidate this receipt.

Rejected earlier attempts were fail-closed boundary evidence: concurrent Task 37 writes changed the source during isolated rebuilding; legacy client connection flags were ignored by Minecraft; incomplete child shutdown was rejected with exit 125 and surviving PIDs; Python unit-test bytecode under `tests/__pycache__` changed dirty identity. Task 39 now invokes `python3 -B` and removed only that task-generated bytecode. The plan checkbox remains open; no commit, issue or PR was created.

## Safety correction and current blocker (2026-09-23)

The five-case result previously called accepted above is **invalid historical forensic evidence**, because `artifact_verify.py` silently wrote `eula=true` without user approval. No further Task 39 dedicated-server/client launch is authorized until explicit user review and approval of Minecraft's EULA. The runner now requires a user-supplied regular `eula.txt` file passed explicitly via `--approved-eula-file` (or `-PfederationApprovedEulaFile` to Gradle), validates its exact bytes before creating runtime paths or binding a port, copies those bytes rather than generating acceptance, and records an approval receipt in fresh runtime evidence. The persisted consumer rejects old receipts. Automatic EULA writes in Task 34/35 server preparation were removed, and scheduled/manual CI does not attempt Task 39 runtime. No fresh server/client result was produced. Task 39 remains BLOCKED and its plan checkbox stays open.

The former CI `runGameTestServer` default selected only `harnessnativesmoke`; a regex accepting any positive count did not establish suite coverage. `tools/required_gametests.py` now enumerates all distinct direct `gametest` backend IDs from the manifest (130 at this snapshot), launches each explicitly and demands exactly one native required-test success for every selected ID. Backend coverage beyond direct GameTests is not claimed. Full CI execution has not been observed locally and requires a CI run.

The direct `runArtifactServer` and `runArtifactClient` Gradle tasks now also have a `taskBefore` EULA approval check, so bypassing the Python runner does not skip preflight. The old persisted five-case result was rechecked non-runtime and rejected by the new approval receipt requirement. The denied Python CLI left no report directory; nine artifact unit tests and strict `check build` passed. No new Minecraft server/client was launched.

## Explicit Task 39 consent input (2026-09-23)

The user's statement `Task39:同意EULA授权` in this session authorizes only Task 39 task-owned, loopback-only temporary server/client verification. The task-owned approval input is `$PWD/.omo/evidence/task-39/eula.txt`; its exact bytes are `eula=true\n` (10 ASCII bytes). Its consent provenance and limits are recorded in `.omo/evidence/task-39/consent-receipt.md`. The Python parser requires a nonsymlink regular file with those exact bytes before any socket or runtime directory. The direct Gradle artifact task prerequisite enforces the same bytes; the shared five-case Gradle dispatch passes the property as `--approved-eula-file` to Python.

After Task 37 source changes stabilize, a later fresh five-case run (not executed here) is:

```bash
./gradlew :neoforge-1.21.1:federationVerify -Pcases=artifact.clean-build,artifact.same-jar-client-server,artifact.license-manifest,artifact.reject-wrong-tuple,artifact.no-extra-target -PevidenceDir=.omo/evidence/task-39 -PfederationApprovedEulaFile=$PWD/.omo/evidence/task-39/eula.txt --dependency-verification=strict --no-configuration-cache --no-daemon
```

This preparation did not launch Minecraft, bind a socket, create a runtime directory or produce accepted artifact evidence. The old unapproved `attempt-20260923T095329159Z` remains invalid; Task 39 stays open until a new source-bound run and persisted verification pass.

## Authorized fresh five-case qualification (2026-09-23)

After confirming no Task 37 native child or `run-gametest` world remained, the exact command above completed in 6m 47s and wrote `.omo/evidence/task-39/attempt-20260923T124941507Z/result.json`. This is a new attempt, not the invalid unapproved `attempt-20260923T095329159Z`. The consent file passed the Python and direct Gradle preflights; `runtime.json` records its SHA-256 `ee27072e4a23e088522f740ddaab0c7c4145c186969e90a86254faa3a5ec5ce6`.

The result is schema v3, complete, with exact five requested/executed `artifact.*` cases and passing assertion counts `4,5,4,3,3`. Run ID `20260923T124941507Z`, source revision `364173f51241fda172a4ff23b1fc3bc0105a214d`, dirty identity `4e62e45b1e258754904f1d967ffb2a0249edcfd20dc44e65b702395416595b42`, source digest `d62329310015c124e74f9ffe5fdbfac0b6a9e75c65f4f3da8d91f34da4167419`, and dependency lock SHA-256 `2677d763489033fdd42a50b0c523cf1bb7d12b158fe71752a394b8d14b382b1b` matched the current checkout at verification. Fresh empty-cache detached-worktree `check build` exited 0 and matched local binary SHA-256 `94f6b657fc9ff1d90d295215ef9bebe665d920c3d8ffb96838f3647a644a69ed` and sources SHA-256 `cf53534424a19db9d112ddc17e2e922d25268140e46b8eb009d8ef3527f9302d`. Archive tuple is MC `[1.21.1]`, NeoForge `[21.1.250]`, AE2 `[19.2.17]`, LDLib2 `[2.2.34]`; the nine negative/archive tests passed.

Both copied installed JARs hash to the binary above. The server and actual Xvfb client each independently logged its installed `mods/` path and that SHA-256; the server logged a loopback `ArtifactProbe` login and server acknowledgment, and the client logged connection to `127.0.0.1:54405` and its acknowledgment. Outer child exits for `artifact-clean`, `artifact-runtime`, and `artifact-negatives` are all 0, with no timeout or surviving descendant in the lifecycle receipt. The temporary detached worktree/cache, runtime directories/world, `run-gametest` directory, listener on port 54405, and owned child processes were absent after the run; installed JAR copies and logs were retained in the attempt. The generic `cleanup-receipt.txt` concerns the GameTest finalizer, while runtime directory cleanup follows the Python runner's `finally` and independent post-run inspection.

The nested server Gradle wrapper printed `Gradle build daemon disappeared unexpectedly` after the server joined, because runtime shutdown terminates owned descendants; this is not a clean nested-wrapper exit even though the outer runtime child exited 0 and authenticated startup/join facts passed. Keep this lifecycle caveat visible in any acceptance review. A client model warning also reports missing `models/item/multipart_bridge.json`; archive integrity and runtime load checks passed, but this smoke does not establish perfect visual asset coverage. Neither issue was changed in this source-bound qualification.

The same-attempt persisted consumer accepted the current checkout:

```bash
./gradlew :neoforge-1.21.1:federationVerifyEvidence -PresultFile=$PWD/.omo/evidence/task-39/attempt-20260923T124941507Z/result.json -PevidenceDir=.omo/evidence/task-39 --dependency-verification=strict --no-configuration-cache --no-daemon
```

Also passed: `PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest discover -s tests -p test_artifact_verify.py -v` (9 tests); `./gradlew :neoforge-1.21.1:check :neoforge-1.21.1:build :neoforge-1.21.1:verifySharedJarContent --dependency-verification=strict --no-configuration-cache --no-daemon`; and `GIT_MASTER=1 git diff --check`. No CI run was claimed. Keep the Task 39 plan checkbox unchanged for Atlas acceptance and regenerate if product source changes.

## Multipart Bridge item resource repair (2026-09-23)

Red runtime evidence: the authorized actual client loaded the former production JAR `94f6b657fc9ff1d90d295215ef9bebe665d920c3d8ffb96838f3647a644a69ed`, then `.omo/evidence/task-39/attempt-20260923T124941507Z/artifact-client.log:67` reported `FileNotFoundException: ae2federation:models/item/multipart_bridge.json`. The former JAR contained `models/part/multipart_bridge.json` but no matching `models/item/multipart_bridge.json`. `BridgeRegistration` registers the `PartItem` as `multipart_bridge`; `MultipartBridgePart.MODEL` separately registers the in-world `part/multipart_bridge`. The missing item path is not resolved by the part registration.

Added only `common/src/main/resources/assets/ae2federation/models/item/multipart_bridge.json` with parent `ae2federation:part/multipart_bridge`. That packaged part model reuses `ae2:part/quartz_fiber` for fiber/particle and `minecraft:block/cyan_concrete` for its north face. Direct ZIP inspection found the quartz fiber PNG (16x16 RGBA) inside pinned `appliedenergistics2-19.2.17.jar` at `assets/ae2/textures/part/quartz_fiber.png`, and the cyan concrete PNG inside cached Minecraft 1.21.1 client JAR at `assets/minecraft/textures/block/cyan_concrete.png`. Existing part geometry is preserved, with no new artwork or registration semantics.

Green packaging evidence: `./gradlew :neoforge-1.21.1:check :neoforge-1.21.1:build :neoforge-1.21.1:verifySharedJarContent --dependency-verification=strict --no-configuration-cache --no-daemon` exited 0. The freshly built `neoforge-1.21.1/build/libs/ae2federation-0.1.0-dev.jar` hashes to `0e50305de0ddcee1d99c37e88e1ef5b8e08342b66e97059e7a9e26aba1e7cfff`; `jar tf` and `unzip -p` showed both the item model and its part parent in this archive. A stdlib ZIP/JSON driver parsed both entries, checked the parent, texture keys and all face references, and located the AE2 texture in its pinned dependency JAR. The asset JSON has no LSP diagnostics and `GIT_MASTER=1 git diff --check` passed. This is static resource resolution, not a rendered-item screenshot or actual-client reload proof.

Do not reuse the old five-case result or its persisted acceptance after this source change. No client/server or full artifact runtime was launched during the concurrent lifecycle work. Atlas must regenerate the source-bound five-case same-JAR result and inspect the actual client model/texture reload log; Task 39 remains open until that and the separate lifecycle concern are adjudicated.

## Nested wrapper lifecycle repair (2026-09-23)

The authorized `attempt-20260923T124941507Z` is **rejected**, not accepted: `artifact-server.log` reports `Gradle build daemon disappeared unexpectedly` and `FAILURE: Build failed with an exception` after the real server/client join. The outer Python process had already written `runtime.json` before its `finally` waited on nested wrappers and discarded their nonzero exits. The persisted Gradle consumer checked the outer exit and joins but not nested exits. This is a reproducible acceptance gap, distinct from the still-unproven explanation for why the daemon died.

A failing-first synthetic runtime test created matching installed JAR hashes and both join markers, then appended the nested failure during server `wait()` and returned exit 1. It failed against the old runner (`ValueError not raised`) and passes after moving report creation behind bounded wrapper/descendant cleanup. The runner records `serverWrapperExit` and `clientWrapperExit` only after both are zero, rejects Gradle build-failure text and any descendant requiring forced SIGKILL, and retains the two logs and installed-JAR copies on rejection. The persisted consumer requires both zero nested exit receipts and rejects build-failure text. A success-path test checks the actual report; a surviving-descendant test confirms no report is written.

`PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_artifact_verify -v` passed 12 tests. `./gradlew help --offline --no-daemon --no-configuration-cache --dependency-verification=strict` passed configuration, and `GIT_MASTER=1 git diff --check` passed. Python error diagnostics were clear; Groovy has no configured LSP. The synthetic tests use no Minecraft processes, real listening socket or external world. A fresh approved five-case actual runtime and persisted consumption remain unrun after this repair and the concurrent model asset change. Do not claim Task 39 acceptance until both pass with the current source identity and real cleanup evidence.

## Current-source actual runtime and persisted verification (2026-09-23)

The first newly approved run with fail-closed nested exits, `attempt-20260923T131257916Z`, is **rejected**. Its server/client both loaded the new item-model JAR (`0e50305de0ddcee1d99c37e88e1ef5b8e08342b66e97059e7a9e26aba1e7cfff`) and joined, and the client no longer logged the missing multipart Bridge model. The server wrapper returned exit 1 with `Gradle build daemon disappeared unexpectedly` and `Shutdown in progress` immediately after the join, while `artifact-runtime.log` reports `ValueError: Artifact server wrapper exit 1`; no `runtime.json`/complete result was accepted. There were no surviving outer descendants.

The existing runner sent SIGTERM to nested descendants before allowing their Gradle wrappers to finish. A synthetic test with an otherwise orderly descendant failed first at that signal (`Gradle daemon received SIGTERM before orderly game exit`). The Task 39 opt-in proof path now requests actual Minecraft client `stop()` on its post-join tick, then the dedicated server `halt(false)` on the tick after that client logs out. Python waits at most 60 seconds for client and then server wrapper exits before retaining the existing emergency descendant cleanup; timeout, nonzero exit, logged build failure and surviving descendants still reject. This required targeted opt-in proof hooks in the two NeoForge entrypoints rather than masking Gradle exit status or adding RCON credentials. All 13 Python tests passed after the change.

The fresh exact five-case command from the consent section above, with `-PfederationApprovedEulaFile=$PWD/.omo/evidence/task-39/eula.txt` and `--dependency-verification=strict --no-configuration-cache --no-daemon`, completed `BUILD SUCCESSFUL in 8m 10s` and produced `.omo/evidence/task-39/attempt-20260923T132715867Z/result.json`. Same-attempt persisted consumption passed:

```bash
./gradlew :neoforge-1.21.1:federationVerifyEvidence -PresultFile=$PWD/.omo/evidence/task-39/attempt-20260923T132715867Z/result.json -PevidenceDir=.omo/evidence/task-39 --dependency-verification=strict --no-configuration-cache --no-daemon
```

The schema-v3 attempt has all five exact cases and assertion counts `4,5,4,3,3`; outer clean/runtime/negative exits and both nested server/client wrapper exits are all zero, with no execution/shutdown timeout or surviving descendants. Server log records actual join, player leave, `Stopping server`, saving chunks and `BUILD SUCCESSFUL`; actual client log records the loopback join, `Stopping!` and `BUILD SUCCESSFUL`. Both runtime paths independently logged the same installed JAR hash as the freshly rebuilt product and retained installed copies: `a7a48cfeb64233eaba3550b71272573bbe65f7dd0bda128a81ca3089f1477da7`. Fresh sources JAR is `fcfc3f126e502162b2574c4f57edab3ec4a819c700f78c2b4604a2707f092616`, source digest `9931c1fd2cce5810742bed608050e8849557164177e31d4618bf30473a251bb6`, and the lock hash is unchanged. The archive contains `assets/ae2federation/models/item/multipart_bridge.json`; its actual-client reload log contains no missing model or texture warning. This does not include a rendered-item visual screenshot.

Strict `:check :build :verifySharedJarContent`, `PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest discover -s tests -p test_artifact_verify.py -v` (13 tests), Python/Java LSP error diagnostics and `GIT_MASTER=1 git diff --check` passed. The task-owned runtime directories/world and GameTest lock were absent, no artifact/GameTest process or listener remained at client-observed port 42917, and `git worktree list` contained only the main checkout. The approved EULA input and consent receipt were preserved unchanged. Task 39 plan checkbox remains open for Atlas; old attempts remain rejected/invalid.

## Independent-review production/dev repair (2026-09-23)

The independent reviewer rejected `attempt-20260923T132715867Z`: `artifactProof` enabled client `Minecraft.stop()` and server `MinecraftServer.halt(false)` inside the distributed NeoForge entrypoints. That result remains rejected despite zero nested exits. Removed the shipped gameplay shutdown state, listeners and calls; retained only the opt-in installed-mod hash and join logging. The task-owned Python runner now sends `WM_DELETE_WINDOW` to the isolated Xvfb client window and, after the client wrapper exits, authenticates to a randomly credentialed RCON endpoint bound only to `127.0.0.1` and issues Minecraft's native `stop` command. The random credential lives only in the removed temporary server configuration, not in logs or source. The runner still rejects timeout, nonzero nested wrapper exits, failed Gradle text and surviving descendants. The production archive inspection rejects shutdown calls in the entrypoint class/source archives; a failing-first test observed `halt` in the old binary, and a mutated archive is rejected.

The first approved external-shutdown attempt, `attempt-20260923T135456171Z`, proved both native exits and normal Gradle completion but failed the persisted producer's literal `Ran 13 tests` check after the negative suite grew to 15. It has no accepted result. Updating that Task 39 check yielded `attempt-20260923T140211225Z`, which passed the producer and consumer. A subsequent direct-script import cleanup changed source identity, so that attempt is superseded, not current proof.

The current exact five-case attempt is `.omo/evidence/task-39/attempt-20260923T141050501Z/result.json`. Both the producer and same-attempt `federationVerifyEvidence` consumer exited zero; all five assertions passed, three outer children and two nested Gradle wrappers exited zero, and no child timeout or surviving descendant was reported. The fresh isolated source digest is `67f1bd6599e6db1d21ae5e650ada1a60632921e66ed40e2c0b830145a0e5be8f`, binary SHA-256 `0e50305de0ddcee1d99c37e88e1ef5b8e08342b66e97059e7a9e26aba1e7cfff`, sources SHA-256 `c9e91dc4260eebd6ecc9d91f23731029502657c8d1917295490b3eb89d93a0d5`, and unchanged dependency-lock digest `2677d763489033fdd42a50b0c523cf1bb7d12b158fe71752a394b8d14b382b1b`. Runtime logs show installed binary hash on both sides, server and client join, actual client `Stopping!`, server `[Rcon: Stopping the server]` and `Stopping server` with saves, and both `BUILD SUCCESSFUL`; no missing model/texture warning was found. The EULA digest is unchanged. Strict `check build verifySharedJarContent`, 15 Python tests, source diagnostics (no errors), CLI `inspect`, `git diff --check` and persisted verification passed. The disposable checkout, runtime directories/world, GameTest runtime and inspected listeners were absent after the run. Do not mark Task 39 complete before an independent second-gate review; the earlier reviewer report remains a historical rejection of the older source.
