# rules

- You should not create any issues and PR by yourself.


## Environment

- Visual asset tooling: user-approved pixi environment at `tools/visual/.pixi/envs/default`; use `pixi run --manifest-path tools/visual/pixi.toml`. Pillow is development-only.
